export const Incomp = () => {
  // 作業中・完了切り替え
  // const onClickComp = (index) => {
  //   const newTasks = [...tasks];
  //   const newCompTasks = [...compTasks, tasks[index]];

  //   newTasks.splice(index, 1);

  //   setCompTasks(newCompTasks);
  //   setTasks(newTasks);
  // };
  return <div>作業中</div>;
};
